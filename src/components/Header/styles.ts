import styled from 'styled-components'

export const HeaderContainer = styled.header`
  nav {
    display: flex;
    padding: 2rem 0;

    a {
      line-height: 0;
      border-radius: 4px;
    }

    img {
      height: 2.5rem;
    }
  }
`
